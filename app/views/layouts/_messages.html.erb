<div class="row">
    <div class="col-mid-12">
        <% flash.each do |message_type, message| %>
          <div class="alert alert-<%= message_type %>"><%= message %>
          <a class="close" data-dismess="alert">&#215;</a>
        </div>
        <% end %>
    </div>
</div>